# Reverse-Shell-Network-Detection-Study
Simulates an active post-compromise egress pivot utilizing an interactive reverse shell stream over an unencrypted transport channel. By mirroring real-world adversary behavior within an isolated virtual local area network (VLAN), this lab captures, isolates, and analyzes raw packet data ($\text{PCAPs}$) via Wireshark
